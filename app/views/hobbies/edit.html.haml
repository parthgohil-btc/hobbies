.row
	.span5.col-md-offset-1
		= form_for(@hobby, url: update_hobbies_path(@hobby.id), method: :post) do |h|
			= render :partial => 'form', locals: { h: h }
			%dt
			%dd
				= h.button 'update', class: "btn btn-primary", disable_with: 'updating...'